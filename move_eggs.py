# from command import ArduinoCommand

def move_egg_out(pos,move_arduino) :
     if move_arduino == None :
          print("can't move out, arduino is none")
     
def hatch(pos,move_arduino) :
     if move_arduino == None :
          print("can't move to hatcher, arduino is none")